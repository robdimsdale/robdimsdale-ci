---
platform: linux
image: docker:///concourse/static-golang

inputs:
- name: sunrise-ci
- name: pivnet-resource
  path: gopath/src/github.com/pivotal-cf-experimental/pivnet-resource
- name: s3-out-binary
- name: version

outputs:
- name: built-binaries
- name: build-output

run:
  path: sunrise-ci/pivnet-resource/build/task

params:
  PIVNET_RESOURCE_DIR: gopath/src/github.com/pivotal-cf-experimental/pivnet-resource
  S3_OUT_BINARY_DIR: s3-out-binary
  VERSION_DIR: version
  BUILT_BINARIES_DIR: built-binaries
  BUILD_OUTPUT_DIR: build-output
