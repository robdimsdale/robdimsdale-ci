---
platform: linux
image: docker:///concourse/static-golang

inputs:
- name: ci-scripts
- name: concourse-pipeline-resource-image
- name: version

outputs:
- name: versioned-rootfs

run:
  path: ci-scripts/concourse-pipeline-resource/version-rootfs/task

params:
  CONCOURSE_PIPELINE_RESOURCE_IMAGE_DIR: concourse-pipeline-resource-image
  VERSION_DIR: version
  VERSIONED_ROOTFS_DIR: versioned-rootfs
