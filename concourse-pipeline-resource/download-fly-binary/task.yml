---
platform: linux
image: docker:///concourse/static-golang

inputs:
- name: sunrise-ci

outputs:
- name: fly-binaries

run:
  path: sunrise-ci/pivnet-resource/download-fly-binary/task

params:
  OUTPUTS_DIR: fly-binaries
