---
platform: linux
image: docker:///concourse/static-golang

inputs:
- name: ci-scripts

outputs:
- name: fly-binaries

run:
  path: ci-scripts/pivnet-resource/download-fly-binary/task

params:
  OUTPUTS_DIR: fly-binaries
