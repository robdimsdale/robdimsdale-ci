---
platform: linux
image: docker:///concourse/static-golang

inputs:
- name: ci-scripts
- name: version
- name: built-binaries

outputs:
- name: versioned-binaries

run:
  path: ci-scripts/pivnet-resource/version-binaries/task

params:
  BUILT_BINARIES_DIR: built-binaries
  VERSION_DIR: version
  VERSIONED_BINARIES_DIR: versioned-binaries
